"use client";
import { useEffect, useState } from "react";
import { BarChart3, RefreshCw, Users } from "lucide-react";
function headers(token:string):Record<string,string>{return token?{"x-admin-token":token}:{}}
type Data={days:number;totalEvents:number;uniqueUsers:number;events:Array<{event:string;count:number;users:number}>;paths:Array<{path:string;count:number;users:number}>};
export function UsageAnalytics({token,canAdmin}:{token:string;canAdmin:boolean}){const [data,setData]=useState<Data|null>(null),[error,setError]=useState("");async function load(){if(!canAdmin){setData(null);return;}try{const r=await fetch("/api/admin/usage?days=30",{headers:headers(token),cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);setData(d);setError("");}catch(e){setError(e instanceof Error?e.message:"Не удалось загрузить usage analytics.");}}useEffect(()=>{void load()},[token,canAdmin]);return <section className="border border-white/10 bg-[#14161a] p-5 sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><BarChart3 className="size-5 text-[#6f91ff]"/><div><p className="text-xs font-bold uppercase tracking-[.14em] text-zinc-500">Business operations</p><h2 className="text-lg font-black text-white">Использование сервиса</h2></div></div><button onClick={()=>void load()} disabled={!canAdmin} className="border border-white/10 p-2 text-zinc-400 disabled:opacity-30"><RefreshCw className="size-4"/></button></div>{!canAdmin?<p className="mt-4 text-sm text-zinc-500">Агрегированная аналитика доступна роли admin.</p>:data?<><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="border border-white/8 bg-black/10 p-4"><p className="text-xs text-zinc-500">События за {data.days} дней</p><p className="mt-1 text-2xl font-black text-white">{data.totalEvents}</p></div><div className="border border-white/8 bg-black/10 p-4"><p className="text-xs text-zinc-500">Уникальные псевдонимы</p><p className="mt-1 flex items-center gap-2 text-2xl font-black text-white"><Users className="size-5 text-[#6f91ff]"/>{data.uniqueUsers}</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.12em] text-zinc-500">Top events</p>{data.events.slice(0,8).map(x=><div key={x.event} className="flex justify-between border-b border-white/6 py-2 text-sm"><span className="text-zinc-300">{x.event}</span><span className="text-zinc-500">{x.count} · {x.users} users</span></div>)}</div><div><p className="mb-2 text-xs font-bold uppercase tracking-[.12em] text-zinc-500">Top pages</p>{data.paths.slice(0,8).map(x=><div key={x.path} className="flex justify-between gap-3 border-b border-white/6 py-2 text-sm"><span className="truncate text-zinc-300">{x.path}</span><span className="shrink-0 text-zinc-500">{x.count}</span></div>)}</div></div><p className="mt-4 text-[11px] leading-5 text-zinc-600">Отчёт агрегирован по HMAC-псевдонимам. E-mail и ФИО в usage_events не сохраняются.</p></>:<p className="mt-4 text-sm text-zinc-500">{error||"Загрузка…"}</p>}</section>}
